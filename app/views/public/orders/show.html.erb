<h1>注文履歴詳細</h1>

注文情報 <br />
注文日
<%= @order.created_at.strftime('%Y/%m/%d') %> <br />

配送先 <br />
<%= @order.postal_code %> <br />
<%= @order.address %> <br />
<%= @order.name %> <br />

支払方法
<%= @order.payment_method_i18n %>  <br />

ステータス
<%= @order.status_i18n %>  <br />


請求情報 <br />
商品合計
<% total = 0 %>
<% @order_details.each do |order_detail| %>
  <% total = total + order_detail.price %>
<% end %>
<%= total.to_s(:delimited) %> <br />
配送料
800 <br />
ご請求金額
<%= @order.total_payment.to_s(:delimited) %>  <br />

注文内容
<table class="table">
  <thead>
    <tr>
      <th>商品</th>
      <th>単価(税込)</th>
      <th>個数</th>
      <th>小計</th>
    </tr>
  </thead>
  <tbody>
    <% @order_details.each do |order_detail| %>
      <tr>
        <td>
          <%= order_detail.item.name %>
        </td>
        <td>
          <%= order_detail.price %>
        </td>
         <td>
          <%= order_detail.amount %>
        </td>
        <td>
          <%= order_detail.price * order_detail.amount %>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>